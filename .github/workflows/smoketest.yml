
name: AQ26 Secret Smoke Test

on:
  workflow_dispatch:
    inputs:
      run_provider_checks:
        description: "Run safe provider smoke checks"
        required: false
        default: "true"

permissions:
  contents: read

concurrency:
  group: aq26-secret-smoketest
  cancel-in-progress: true

jobs:
  smoketest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Safe note
        run: echo "Manual-only smoke test placeholder. Run WeeklyV2 SCC Nexus Website for production."
