name: Disabled AQ26 Commit Push Replacement Snippet

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled placeholder
        run: |
          echo "This file used to contain a workflow step snippet, not a complete workflow."
          echo "It is now a valid manual-only placeholder so GitHub Actions YAML validation passes."
          echo "No production deployment or generated-output commit is performed here."
